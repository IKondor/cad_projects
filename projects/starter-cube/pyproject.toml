[project]
name = "starter-cube"
version = "0.1.0"
description = "Шаблонный проект: куб 2×2×2 м"
requires-python = ">=3.12,<3.13"
dependencies = ["cadkit"]

# Проекты — не пакеты, а наборы скриптов: собирать их незачем.
[tool.uv]
package = false

[tool.uv.sources]
cadkit = { workspace = true }
